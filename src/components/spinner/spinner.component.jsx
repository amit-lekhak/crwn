import React from 'react';
import { SpinnerContainer, SpinnerOverLayContainer } from './spinner.styles';

const Spinner = () => (
  <SpinnerOverLayContainer>
    <SpinnerContainer />
  </SpinnerOverLayContainer>
);

export default Spinner;
